module.exports = {
    name: "connect",
    description: "connecte le bote a un chanel vocal",
    execute(client,msg, arg){
        if (msg.author.tag == "NONO70063#4609") {
            
        }else{
            msg.reply(`La commande ${this.name} est en cour de devellopement`);
        }
    }
}